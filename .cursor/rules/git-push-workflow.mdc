---
description: 當用戶說「確定，幫我推版」時的 Git 自動化工作流程
alwaysApply: true
---

# Git 推版工作流程

當用戶說「**確定，幫我推版**」時，表示用戶已經瀏覽過修改範圍，請執行以下步驟：

## 執行步驟

1. **Stage 所有變更**
   ```bash
   git add .
   ```

2. **Amend 到上一個 commit**
   ```bash
   git commit --amend --no-edit
   ```
   - 這會觸發 pre-commit hook（format、prettier、lint）
   - 等待 hook 完成

3. **Push 到遠端**
   ```bash
   git push
   ```

4. **如果 push 失敗（分支落後）**
   ```bash
   git pull --rebase
   git push
   ```

## 注意事項

- 如果 pre-commit hook 失敗，停止流程並回報錯誤給用戶
- 如果 rebase 有衝突，停止流程並告知用戶需要手動處理
- 每個步驟完成後，確認指令成功再繼續下一步
