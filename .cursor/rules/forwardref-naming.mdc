---
description: React 19 ref 規範 - 使用 ref-as-prop 取代 forwardRef
globs: "**/*.tsx"
alwaysApply: false
---

# React 19 Ref 規範

## 核心原則

專案使用 React 19.x，`forwardRef` 已被標記為 deprecated。**禁止在新組件中使用 `forwardRef`**。所有需要 ref 的組件一律在 props 介面加入 `ref?: React.Ref<T>`，使用 ref-as-prop 模式。

## 規則

### 1. 新組件：ref-as-prop
- 使用 `React.ComponentProps<"element">` 或自訂 props type 包含 `ref`
- `ref` 直接從 props 解構，作為普通 prop 傳遞

### 2. 現有 forwardRef 組件：逐步遷移
- 當修改既有使用 `forwardRef` 的組件時，順帶遷移為 ref-as-prop
- 不需要一次性全部重構

### 3. Radix UI 包裝層
- 包裝 Radix primitive 時，使用 `React.ComponentProps<typeof RadixPrimitive>` 取得含 ref 的完整型別
- 將 ref 作為普通 prop 傳遞給 Radix primitive

## 範例

### 原生 HTML 元素包裝

```typescript
// ✅ React 19 ref-as-prop（參照專案 Input 組件寫法）
function Input({ className, type, ref, ...props }: React.ComponentProps<"input">) {
  return (
    <input
      ref={ref}
      type={type}
      className={cn("base-styles", className)}
      {...props}
    />
  );
}
```

### Radix UI 包裝

```typescript
// ✅ React 19 ref-as-prop
function DialogTitle({ className, ref, ...props }: React.ComponentProps<typeof RadixDialog.Title>) {
  return (
    <RadixDialog.Title
      ref={ref}
      className={cn("text-lg font-semibold", className)}
      {...props}
    />
  );
}

// ❌ 不再使用 forwardRef
const DialogTitle = forwardRef<
  React.ElementRef<typeof RadixDialog.Title>,
  React.ComponentPropsWithoutRef<typeof RadixDialog.Title>
>(function DialogTitleForwardRef({ className, ...props }, ref) {
  return <RadixDialog.Title ref={ref} className={cn("...", className)} {...props} />;
});
```

### 自訂 Props 需要 ref

```typescript
// ✅ 明確加入 ref 型別
type DialogContentProps = Pick<DialogProps, "className" | "children" | "size"> & {
  ref?: React.Ref<React.ElementRef<typeof RadixDialog.Content>>;
};

function DialogContent({ ref, size = "md", children, className }: DialogContentProps) {
  return (
    <RadixDialog.Content ref={ref} className={cn(getSizeClass(size), className)}>
      {children}
    </RadixDialog.Content>
  );
}
```

## 遷移檢查清單

將 `forwardRef` 遷移為 ref-as-prop 時：

1. 移除 `forwardRef` 包裝，改為普通命名函式
2. 型別從 `ComponentPropsWithoutRef` 改為 `ComponentProps`（含 ref）
3. `ref` 從函式第二參數移至 props 解構
4. 保留 `.displayName` 設定（compound component 或 Radix 包裝層在 DevTools 中仍需要明確名稱）
5. 確認 ref 仍正確傳遞給底層元素

## 待遷移組件

| 組件 | 檔案 | 狀態 |
|------|------|------|
| `DialogOverlay` | `src/ui/dialog.tsx` | ✅ 已完成 |
| `DialogContent` | `src/ui/dialog.tsx` | ✅ 已完成 |
| `DialogTitle` | `src/ui/dialog.tsx` | ✅ 已完成 |
| `DialogDescription` | `src/ui/dialog.tsx` | ✅ 已完成 |
| `PopoverContent` | `src/ui/popover.tsx` | ✅ 已完成 |
