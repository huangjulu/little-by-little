---
description: 狀態管理規則 - 依據狀態類型選擇對應工具
globs: "**/*.tsx,**/*.ts"
alwaysApply: false
---

# 狀態管理規範

## 核心原則

依據狀態的性質選擇對應的管理工具，不引入不必要的外部狀態庫。

## 狀態分層與工具選擇

| 狀態類型 | 使用工具 | 範例 |
|---------|---------|------|
| 組件內部 UI 狀態 | `useState` | input focus、dropdown open、keyword |
| 伺服器資料快取 | `@tanstack/react-query` | API 資料、列表、詳情 |
| 跨元件共享狀態 | React Query `queryClient` 或提升 state 到共同父層 | 篩選條件、已選項目 |

## 規則

### 1. 組件內部 UI 狀態 → `useState`
- 組件自身的互動狀態（如 `keyword`、`statusFilter`、`isOpen`）使用 `useState` 管理
- 透過 callback props 通知父層（如 `onFiltersChange`、`onOrderSelect`）

### 2. 伺服器資料 → `@tanstack/react-query`
- 所有 API 資料的 fetching、caching、synchronizing 使用 React Query
- 使用 `useQuery` 讀取、`useMutation` 寫入
- API 相關的資料抓取只在 Page 層級進行

### 3. 跨元件共享狀態
- **優先**使用 React Query 的 `queryClient.setQueryData` 來共享伺服器資料的衍生狀態
- **其次**將 state 提升到共同父層，透過 props 向下傳遞
- **不需要**額外引入全域狀態管理庫

## 範例

### React Query 管理伺服器資料

```typescript
// useOrders.ts
import { useQuery, useMutation, useQueryClient } from "@tanstack/react-query";

export function useOrders() {
  return useQuery({
    queryKey: ["orders"],
    queryFn: fetchOrders,
  });
}

export function useUpdateOrder() {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: updateOrder,
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: ["orders"] });
    },
  });
}
```

### 跨元件共享：提升 state 到父層

```typescript
// Page 層級管理共享狀態，向下傳遞
export const OrderPage: React.FC = () => {
  const [selectedIds, setSelectedIds] = useState<string[]>([]);
  const { data: orders } = useOrders();

  return (
    <>
      <OrderFilters selectedIds={selectedIds} />
      <OrderTable
        orders={orders}
        selectedIds={selectedIds}
        onSelectionChange={setSelectedIds}
      />
    </>
  );
};
```

### 跨元件共享：React Query setQueryData

```typescript
// 透過 queryClient 共享衍生狀態
const queryClient = useQueryClient();

function handleSelectOrder(id: string) {
  queryClient.setQueryData<string[]>(["selectedOrders"], (prev = []) => {
    return prev.includes(id)
      ? prev.filter((i) => i !== id)
      : [...prev, id];
  });
}

// 其他元件讀取
const selectedIds = useQuery({
  queryKey: ["selectedOrders"],
  initialData: [],
});
```

## 未來擴充

如果未來需要引入 Redux 等全域狀態管理庫（例如複雜的購物車、多步驟表單跨頁共享），請先：
1. 建立 `src/store/` 目錄結構
2. 更新此規則文件
3. 在 `development.mdc` 中同步說明
