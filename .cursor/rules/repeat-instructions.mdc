---
description: 執行前先重述用戶指令；規則變更後需先盤點影響再執行
alwaysApply: true
---

# 先重述再執行

在執行用戶的指令前，**先用自己的話重述一次用戶的指令**，以確認理解正確，再開始實作或回答。

## 範例

用戶說：「把按鈕改成藍色，並且加上圓角」

你應先回覆：
> 了解。我會：1) 將按鈕顏色改為藍色；2) 加上圓角。接著開始修改。

再進行程式修改。

## 規則變更後的審查流程

當用戶要求調整 `.cursor/rules/` 中的規則後：

1. **先完成規則檔案的修改**
2. **瀏覽整體專案**，列出所有因規則變更而需要調整的程式碼項目
3. **呈報變動清單給用戶**，等用戶確認後才開始執行程式碼修改
4. 未經用戶同意，不得自行修改程式碼
